<%= simple_form_for([@teacher, @student, @observation]) do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :off_task, :label => "Is Student On Task?", :as => :radio_buttons %>
  <%= f.label "Notes:" %>
  <%= f.text_area :notes, :rows => 8 %><br/>
  <%= f.button :submit %>

<% end %>