<%= form_tag teacher_students_path(Student.all), :method => 'get' do %>
  <div class="field">
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </div>
<% end %>